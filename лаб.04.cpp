/*
    Лабораторная работа №4
    Вариант 26
    Студент Останин С.А; группа ПИ-Э
    Задача:
    Дано натуральное число N, не превосходящее 365. Найти число и 
    месяц невисокосного года, на который приходится N-ый по счёту 
    день этого года. Массивами не пользоваться. 
    Усложнение 1: операторами циклов и рекурсией не пользоваться.
    Усложнение 2: операторами циклов, ветвления, выбора и рекурсией не пользоваться.
*/

#include <iostream>
#include <locale.h>
using namespace std;

int main(){
setlocale(LC_ALL, "Russian");
    int N;

    cout << "Введите день в году: "<<endl;
    cin >> N;

    if ((N < 1) || (N > 365)) {
        cout << "Ошибка: число должно быть в диапазоне от 1 до 365."<<endl;
        return 1;
    }

    // Определяем месяц и день
    int day;
    int month;

    if (N <= 31) {
        month = 1; // Январь
        day = N;
    } else if (N <= 59) {
        month = 2; // Февраль
        day = N - 31;
    } else if (N <= 90) {
        month = 3; // Март
        day = N - 59;
    } else if (N <= 120) {
        month = 4; // Апрель
        day = N - 90;
    } else if (N <= 151) {
        month = 5; // Май
        day = N - 120;
    } else if (N <= 181) {
        month = 6; // Июнь
        day = N - 151;
    } else if (N <= 212) {
        month = 7; // Июль
        day = N - 181;
    } else if (N <= 243) {
        month = 8; // Август
        day = N - 212;
    } else if (N <= 273) {
        month = 9; // Сентябрь
        day = N - 243;
    } else if (N <= 304) {
        month = 10; // Октябрь
        day = N - 273;
    } else if (N <= 334) {
        month = 11; // Ноябрь
        day = N - 304;
    } else {
        month = 12; // Декабрь
        day = N - 334;
    }

    cout << "День: " << day << ", Месяц: " << month << endl;
}